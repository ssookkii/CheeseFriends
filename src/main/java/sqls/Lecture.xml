<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mul.cam.a.dao.LectureDao">

<!-- 강의 목록 가져오기 -->
<select id="lectureList" parameterType="mul.cam.a.dto.LectureParam" resultType="mul.cam.a.dto.LectureDto">
	SELECT seq, subject, subjectCode, title, writer, content, regdate
	FROM lecture
</select>

<!-- 강의 글쓰기 -->
<insert id="writeLecture" parameterType="mul.cam.a.dto.LectureDto">
	INSERT INTO lecture(subject, subjectCode, title, writer, content, regdate)
	VALUES(#{subject}, #{subjectCode}, #{title}, #{writer}, #{content}, now())
</insert>


</mapper>